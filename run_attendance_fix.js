const fs = require('fs');
const path = require('path');

try {
  const sqlContent = fs.readFileSync('complete_attendance_fix.sql', 'utf8');

} catch (error) {

}

const path = require('path');

try {
  const sqlContent = fs.readFileSync('complete_attendance_fix.sql', 'utf8');

} catch (error) {

}

const path = require('path');

try {
  const sqlContent = fs.readFileSync('complete_attendance_fix.sql', 'utf8');

} catch (error) {

}

